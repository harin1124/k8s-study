apiVersion: v1
kind: ConfigMap
metadata:
  name: todo-web-config-test
  namespace: test
data:
  config.json: |-
    {
      "ConfigController": {
        "Enabled" : false
      },
      "Database" : {
        "Provider" : "Postgres"
      }
    }